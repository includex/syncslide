# 2026-06-14 Work Log

## 작업 배경

- 제보된 현상: 회원가입 시 브라우저에서 `Failed to fetch`가 표시되며 가입이 진행되지 않음

## 확인 내용

- 프런트엔드 회원가입 호출 경로는 [apps/web/lib/api.ts](/Users/ain/workspace/syncslide/apps/web/lib/api.ts)에서 처리되고 있었음
- 가입/로그인 UI는 [apps/web/app/login/page.tsx](/Users/ain/workspace/syncslide/apps/web/app/login/page.tsx)에서 동일한 API 래퍼를 사용하고 있었음
- 백엔드 인증 라우트는 [apps/server/src/routes/auth.ts](/Users/ain/workspace/syncslide/apps/server/src/routes/auth.ts)에 존재하며, 라우트 자체는 `/api/auth/register` 및 `/api/auth/login`으로 정상 구성되어 있었음
- 기존 프런트 기본 API 주소는 `http://localhost:4000` 고정값이었음

## 원인 판단

- 브라우저에서 직접 `http://localhost:4000`으로 요청하는 구조는 접속 환경에 따라 쉽게 깨짐
- 특히 `localhost`는 사용자 브라우저 기준이므로 다음 조건에서 `Failed to fetch`가 발생할 수 있음
- 웹 앱 접속 호스트가 `127.0.0.1`, 다른 도메인, 모바일 기기, 외부 네트워크 주소인 경우
- CORS 설정과 실제 접속 origin이 어긋나는 경우
- 결과적으로 회원가입 요청이 백엔드 응답 전에 네트워크 계층에서 실패할 가능성이 높다고 판단

## 적용한 수정

### 1. API 호출 기본 경로 변경

- 파일: [apps/web/lib/api.ts](/Users/ain/workspace/syncslide/apps/web/lib/api.ts)
- `NEXT_PUBLIC_API_URL`이 없을 때 기본값을 `http://localhost:4000`에서 빈 문자열로 변경
- 브라우저가 같은 origin의 `/api/...` 경로를 호출하도록 수정
- fetch 자체 실패 시 사용자에게 `서버에 연결할 수 없습니다. 백엔드 서버 주소를 확인해 주세요.` 메시지를 표시하도록 처리 추가

### 2. Next.js API 프록시 추가

- 파일: [apps/web/next.config.mjs](/Users/ain/workspace/syncslide/apps/web/next.config.mjs)
- `/api/:path*` 요청을 백엔드 API 서버로 전달하는 `rewrites()` 추가
- 프록시 대상은 `API_PROXY_TARGET`, 없으면 `NEXT_PUBLIC_API_URL`, 둘 다 없으면 `http://localhost:4000`을 사용하도록 구성

## 검증 결과

- `npm run typecheck --workspace=apps/web` 실행 통과
- 정적 타입 기준으로 이번 수정에 따른 프런트 회귀는 확인되지 않음

## 반영 시 주의사항

- `next.config.mjs` 변경이 포함되어 있으므로 웹 개발 서버 재시작이 필요함

## 남은 리스크

- 소켓 연결 코드는 [apps/web/lib/socket.ts](/Users/ain/workspace/syncslide/apps/web/lib/socket.ts)에서 아직 `http://localhost:4000` 기본값을 사용 중임
- 회원가입 문제와 별개로, 실시간 기능은 동일한 환경 차이에서 연결 실패 가능성이 남아 있음
